
# TXIO Sizer – Upload‑only (full)

**Funktional:** Eingabemaske, Berechnung, Ergebnis‑Tabelle (SAP‑Layout), Rechenweg, Geräte‑Info.  
**Deploy:** Nur `/docs` hochladen → Pages auf `/docs` stellen → URL aufrufen.

URL‑Schema (Project Page): `https://<user>.github.io/<REPO_NAME>/`
